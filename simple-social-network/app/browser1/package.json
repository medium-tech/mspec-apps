{
    "name": "simple_social_network",
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "test": "npx playwright test",
        "test-ui": "npx playwright test --ui",
        "test-report": "npx playwright show-report",
        "test-gen": "npx playwright codegen http://localhost:8008"
    },
    "devDependencies": {
        "@playwright/test": "1.52.0",
        "loadtest": "^8.2.0"
    }
}